<%= form_for(current_user.offer_active_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :of_cand_id, @offer.id %></div>
    <%= f.submit "Candidatar-me", class: "btn btn-default" %>
<% end %>
