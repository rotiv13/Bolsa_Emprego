<% if @entitie.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger">
        O formulário contém <%= pluralize(@entitie.errors.count, "erro") %>.
      </div>
      <ul>
        <% @entitie.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>